<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egoksg.migration.db1.mapper.TestDb1Mapper">
  <select id="select" resultType="TestDb1Dto">
    SELECT
      NO
     ,NAME
    FROM
      TB_TEST_DB1
  </select>
  
  <insert id="insert" parameterType="TestDb1Dto">
    INSERT TB_TEST_DB1 (
      NAME
    ) VALUES (
      #{name}
    )
  </insert>
</mapper>